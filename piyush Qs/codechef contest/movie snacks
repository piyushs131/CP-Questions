#include <iostream>
using namespace std;

int main() {
    int res, ans, cnt;
    cin >> res >> ans >> cnt;

    int sum = 2 * res + 3 * ans;
    int min_cost = 2 * cnt + ans;
    int final_res = min(sum, min_cost);

    cout << final_res << endl;

    return 0;
}