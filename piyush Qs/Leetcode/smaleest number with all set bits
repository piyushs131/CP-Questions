class Solution {
public:
    int smallestNumber(int n) {
        for(int i = 1; i <= 10; i++){
            int x = pow(2, i) - 1;
            if(x >= n) return x;
        }
        return -1;
    }
};