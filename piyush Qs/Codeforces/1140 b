#include<bits/stdc++.h>
using namespace std;
string s;
int main()
{
    int i,t,n;
    cin>>n;
    while(n--)
    {
        cin>>t>>s;
        for(i=0;i<t;i++)
        if(s[i]=='>' || s[t-i-1]=='<')
        {
        cout<<i<<endl;
        break;
        }
    }
    
}