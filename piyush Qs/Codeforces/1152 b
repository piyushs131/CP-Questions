#include<bits/stdc++.h>
using namespace std;
int j=19,k;
main()
{cout<<"39"<<endl;
    int a;
    cin>>a;
    while(j--)
    {
        k=log2(a)+1;
        a^=(int)(pow(2,k)-1);
        a++;
        cout<<k<<' ';
    }
    cout<<log2(a)+1;
}