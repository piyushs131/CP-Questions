#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n,x,y,c=0;
	cin>>n>>x>>y;
	int arr[n];
	for (int i = 0; i < n; ++i)
	{
		cin>>arr[i];
		if(arr[i]<=x)
			c++;
	}
	if(x>y)cout<<n<<endl;
	else
		cout<<(c+1)/2<<endl;
} 