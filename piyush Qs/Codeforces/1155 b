#include <bits/stdc++.h>
 
using namespace std;
 
int main(){
	int n, e = 0;
	string s;
	cin >> n >> s;
	for (int i = 0; i <= s.size() - 11; ++i)
		if(s[i] == '8')
			e++;
	if(((n - 11)/2) >= e)
		cout << "NO";
	else
		cout << "YES";
}