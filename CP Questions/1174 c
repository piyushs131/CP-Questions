#include<bits/stdc++.h>
using namespace std;
 
int main()
{
	int n,count=0;
	cin>>n;
	
	vector<int> v(n+1,0);
	
	for(int i=2;i<=n;i++)
	{
		if(v[i]==0)
		{
			count++;
			
			for(int j=i;j<=n;j=j+i)
			v[j]=count;
		}
		
		cout<<v[i]<<" ";
	}
	
	return 0;
}